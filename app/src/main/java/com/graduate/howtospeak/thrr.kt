package com.graduate.howtospeak.com.graduate.howtospeak





/*
var thread: Thread

private fun startListenAudio() {
    thread = Thread {
        while (isThreadRun) {
            try {
                if (bListener) {
                    volume = mRecorder.getMaxAmplitude() //Get sound pressure value
                    if (volume > 0 && volume < 1000000) {
                        World.dbCount =
                            20 * Math.log10(volume)
                                .toFloat() //Convert sound pressure value to decibel value
                    }
                }
                Thread.sleep(100)
            } catch (e: InterruptedException) {
                e.printStackTrace()
                bListener = false
            }
        }
    }
    thread.start()
}

 */