package com.graduate.howtospeak.com.graduate.howtospeak

/*
class World {
    var dbCount = 40f

    private var lastDbCount = dbCount
    private val min = 0.5f
    private var value = 0f

    @JvmName("setDbCount1")
    fun setDbCount(dbValue: Float) {
        value = if (dbValue > lastDbCount) {
            if (dbValue - lastDbCount > min) dbValue - lastDbCount else min
        } else {
            if (dbValue - lastDbCount < -min) dbValue - lastDbCount else -min
        }
        dbCount = lastDbCount + value * 0.2f
        lastDbCount = dbCount
    }
}

 */